{% spaceless %}
    <div class="schedule-layout schedule-layout--grid">
        <div class="row" data-studio-slots-container>
            <div class="col-md-6 col-sm-12 schedule-layout--hero__slot schedule-layout__slot" data-studio-slot>
                {% if items[0] is defined %}
                    {{ render(controller('ez_content:viewAction', {
                        'locationId': items[0],
                        'viewType': 'grid-2'
                    })) }}
                {% endif %}
            </div>
            <div class="col-md-6 col-sm-12 schedule-layout--hero__slot schedule-layout__slot" data-studio-slot>
                {% if items[1] is defined %}
                    {{ render(controller('ez_content:viewAction', {
                        'locationId': items[1],
                        'viewType': 'grid-2'
                    })) }}
                {% endif %}
            </div>
        </div>
        <div class="row" data-studio-slots-container>
            <div class="col-md-6 col-sm-12 schedule-layout--hero__slot schedule-layout__slot" data-studio-slot>
                {% if items[2] is defined %}
                    {{ render( controller('ez_content:viewAction', {
                        'locationId': items[2],
                        'viewType': 'grid-2'
                    })) }}
                {% endif %}
            </div>
            <div class="col-md-6 col-sm-12 schedule-layout--hero__slot schedule-layout__slot" data-studio-slot>
                {% if items[3] is defined %}
                    {{ render(controller('ez_content:viewAction', {
                        'locationId': items[3],
                        'viewType': 'grid-2'
                    })) }}
                {% endif %}
            </div>
        </div>
    </div>
{% endspaceless %}
